doctype html
html
  head
    title Image Viewer Genjutsu!
  body
    h1 Image Viewer!!!
    div Search returns first 100 results!
    div.loader
    div
      input(type='text' id='search-tbx')
      button(id='search-btn') Search
      div(id='light-box' class='turn-off')
        div.title-holder
          div.title-container
            h1.img-title
          div.navigation-btns
            div.action-btn.previous
            div.action-btn.next
        div.right-section
          div.close-container
            div.action-btn.close
          div.image-frame
      div.image-grid
